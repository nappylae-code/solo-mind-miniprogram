<view class="container">

<!-- 搜索框 -->
<view class="search-bar">
  <view class="search-inner">
    <text class="search-icon">🔍</text>
    <input
      class="search-input"
      placeholder="搜索日记..."
      placeholder-style="color: #BDBDBD;"
      value="{{searchKeyword}}"
      bindinput="onSearchInput"
    />
    <text
      class="search-clear"
      wx:if="{{searchKeyword}}"
      bindtap="onSearchClear"
    >✕</text>
  </view>
</view>

<!-- 统计摘要栏：无日记时自动隐藏 -->
<view class="summary-bar" wx:if="{{summary.hasData}}">
  <text class="summary-text">共 {{summary.totalCount}} 篇日记</text>
  <text class="summary-dot">·</text>
  <text class="summary-text">连续记录 {{summary.streakDays}} 天</text>
</view>

<!-- 日记列表 -->
<scroll-view scroll-y class="scroll-content">

  <block wx:if="{{monthGroups.length > 0}}">
    <block wx:for="{{monthGroups}}" wx:key="monthLabel">
      <view class="month-header">
        <text class="month-label">📅 {{item.monthLabel}}</text>
      </view>
      <block wx:for="{{item.entries}}" wx:key="date" wx:for-item="entry">
        <view
          class="entry-card"
          bindtap="onEntryTap"
          data-date="{{entry.date}}"
        >
          <!-- ✅ entry-left（情绪图标）已移除 -->
          <view class="entry-middle">
            <text class="entry-date">{{entry.displayDate}}</text>
            <text class="entry-preview">{{entry.preview}}</text>
          </view>
          <view class="entry-right">
            <text class="entry-arrow">→</text>
          </view>
        </view>
      </block>
    </block>
  </block>

  <view class="empty-state" wx:elif="{{!loading}}">
    <text class="empty-emoji">📖</text>
    <text class="empty-text">还没有日记</text>
    <text class="empty-sub">点击右下角按钮写下今天的心情</text>
  </view>

  <view class="bottom-spacer"></view>
</scroll-view>

<!-- FAB -->
<view class="fab" bindtap="onNewDiary">
  <text class="fab-icon">✏️</text>
</view>

</view>